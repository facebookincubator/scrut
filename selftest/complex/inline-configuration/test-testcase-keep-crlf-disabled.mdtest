# This test explicitly disables keep crlf

Helper to validate that the command line parameter `--keep-output-crlf` overrides inline per-testcase configuration

## Run test

```scrut {keep_crlf: false}
$ echo -e "word\r"
word
```
