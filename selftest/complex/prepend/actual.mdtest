# The actual test

Moon Cram supports prepending of tests in front of a list of other files.

## Run test with the assumption that prepended.mdtest is injected

```mooncram
$ echo "Good things are ${SOME_VARIABLE:-undefined}"
Good things are prepended, not shaken
```
