# @generated by autocargo from //clifoundation/scrut:[library,scrut-oss]

[package]
name = "scrut"
version = "0.4.1"
edition = "2024"
rust-version = "1.85"
description = "A simple and powerful test framework for CLI applications"
readme = "README.md"
homepage = "https://facebookincubator.github.io/scrut/"
repository = "https://github.com/facebookincubator/scrut"
license = "MIT"
keywords = ["cli", "testing"]
categories = ["command-line-utilities", "development-tools::testing"]

[[bin]]
name = "scrut"
path = "src/bin/main.rs"

[dependencies]
anyhow = "1.0.98"
clap = { version = "4.5.42", features = ["derive", "env", "string", "unicode", "wrap_help"] }
console = "0.15.7"
derivative = "2.2"
derive_builder = { version = "0.20.2", features = ["alloc"] }
dialoguer = { version = "0.10.4", features = ["fuzzy-select"] }
dunce = "1.0.5"
globset = { version = "0.4.13", features = ["serde1"] }
humantime = "2.1"
indicatif = { version = "0.18.3", features = ["futures", "improved_unicode", "rayon", "tokio"] }
lazy_static = "1.5"
rand = { version = "0.8", features = ["small_rng"] }
regex = "1.11.1"
serde = { version = "1.0.219", features = ["derive", "rc"] }
serde_json = { version = "1.0.140", features = ["alloc", "float_roundtrip", "raw_value", "unbounded_depth"] }
serde_yaml = "0.9.25"
shell-escape = "0.1.5"
strip-ansi-escapes = "0.1"
subprocess = "0.2.9"
tempfile = "3.22"
thiserror = "2.0.12"
tracing = { version = "0.1.41", features = ["attributes", "valuable"] }
tracing-subscriber = { version = "0.3.20", features = ["chrono", "env-filter", "json", "local-time", "parking_lot", "registry"] }
unicode_categories = "0.1.1"
which = "4.2.4"
wildmatch = "2.1"

[dev-dependencies]
insta = { version = "1.34", features = ["colors", "console", "default", "glob", "globset", "json", "serde", "walkdir"] }

[build-dependencies]
vergen = "7"

[target.'cfg(target_os = "linux")'.dependencies]
nix = { version = "0.30.1", features = ["dir", "event", "hostname", "inotify", "ioctl", "mman", "mount", "net", "poll", "ptrace", "reboot", "resource", "sched", "signal", "term", "time", "user", "zerocopy"] }

[target.'cfg(target_os = "macos")'.dependencies]
nix = { version = "0.30.1", features = ["dir", "event", "hostname", "inotify", "ioctl", "mman", "mount", "net", "poll", "ptrace", "reboot", "resource", "sched", "signal", "term", "time", "user", "zerocopy"] }

[features]
default = ["logging"]
logging = []
volatile_tests = []

[profile.release]
lto = true
strip = true
